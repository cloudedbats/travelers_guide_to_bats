name: travelers_guide_to_bats
description: "Traveler's Guide to Bats. Developed in Flutter and Dart."
publish_to: 'none'
version: 2024.1.1-development

environment:
  sdk: ^3.5.0

dependencies:
  flutter_bloc:
  equatable:
  hive:
  hive_flutter:
  path_provider:
  shared_preferences:
  package_info_plus:
  excel:
  flutter_html:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator:
  build_runner:
  flutter_lints: 

flutter:
  uses-material-design: true

  assets:
    - assets/files/
    - assets/icons/
